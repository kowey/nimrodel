%% nimrodel.VERSION

#hide nimrodel.VERSION.
nimrodel.VERSION:
	<> == elf.VERSION
	<display> == nimrodel.display.VERSION

	<package> == 'nimrodel - TTT NLP module'
	<appname> == nimrodel
	<major> == 0
	<minor> == 3
	<date> == '08 January 2015'
.

#hide nimrodel.display.VERSION.
nimrodel.display.VERSION:
	<> == elf.display.VERSION
	<display> == "<package>" <space>
		version <space>
		"<major>" \. "<minor>" <space>
		( "<date>" )
.
