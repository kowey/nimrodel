% null output (dump of intermediary semantics)

#uses Gensym Length Implode Explode LocalPath Rest.


app.DUMP:
	<> == elf.TOP
	<sp> == ' '
	<nl> == '\n'
		
	<dump title> == <unwords> <nl>
	<dump sentence $documentString> == <unwords> <nl>
	
	<unwords $any> == $any <sp> <unwords>
	<unwords> ==
.
