#!/bin/bash

# nimrodel-repeat iterations file
# repeatedly run nimrodel on the same file

. "${0%/*}/env"

export JAVA_TOOL_OPTIONS="-XX:+HeapDumpOnOutOfMemoryError"

"$SWIPL" -q -f "$ELFAPP"/swiprolog.pl -g time_repeat_on_file -t halt -- "$@"
