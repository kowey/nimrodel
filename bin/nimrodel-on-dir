#!/bin/bash

# nimrodel-on-dir input-dir output-dir
# recursively traverse the input directory; write to an
# equivalent output file in the output directory

. "${0%/*}/env"

"$SWIPL" -q -f "$ELFAPP"/swiprolog.pl -g traverse_dir -t halt -- "$@"
